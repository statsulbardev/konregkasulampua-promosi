---
title: "Informasi Tempat Wisata Sulbar"
description: Informasi tempat wisata di Sulawesi Barat
type: posts
---

import { Objekwisata } from "../../components/pariwisata/objekwisata";
import { getPagesUnderRoute } from "nextra/context";
import Link from "next/link";
import Image from "next/image";

export function WisataIndex() {
  return getPagesUnderRoute("/paketwisata").map((page) => {
    return (
      <div key={page.route} className="mb-10">
        <Link href={page.route}>
          <Image
            className="mt-8 block"
            src={page.frontMatter?.symbol}
            width="1100"
            height="165"
            alt="image"
          />
        </Link>
        <Link
          href={page.route}
          style={{ color: "inherit", textDecoration: "none" }}
          className="mt-2 block text-2xl font-bold"
        >
          {page.meta?.title || page.frontMatter?.title || page.name}
        </Link>
        <p className="text-sm opacity-80" style={{ marginTop: ".5rem" }}>
          Destinasi: {page.frontMatter?.tag}{" "}
        </p>
        {page.frontMatter?.date ? (
          <p className="text-sm opacity-80">Jadwal: {page.frontMatter.date}</p>
        ) : null}
        <span className="inline-block text-sm font-medium text-blue-500">
          <Link href={page.route}>{"Info Selengkapnya →"}</Link>
        </span>
      </div>
    );
  });
}

## Paket Wisata

<WisataIndex />

## Daftar Destinasi Wisata di Sulawesi Tenggara

<Objekwisata />
