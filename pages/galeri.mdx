---
description: Berita Terkait Kegiatan Konsultasi Regional PDRB Wilayah Kasulampua 2024
type: posts
---

import { getPagesUnderRoute } from "nextra/context";
import Link from "next/link";

export function BeritaHeader() {
  return (
    <div className="mx-auto mb-16 max-w-screen-lg border-b border-gray-400 border-opacity-20 pb-8 pt-4">
      <h1>
        <p className="text-md mb-6 text-center font-bold lg:text-5xl">Galeri</p>
      </h1>
      <p className="text-center font-space-grotesk text-gray-500 dark:text-gray-400">
        Unduh foto-foto selama kegiatan Konreg Kasulampua 2024
      </p>
    </div>
  );
}

export function BeritaIndex() {
  return getPagesUnderRoute("/galeri").map((page) => {
    return (
      <div key={page.route} className="mb-1">
        <Link
          href={page.route}
          style={{ color: "inherit", textDecoration: "none" }}
          className="mt-8 block text-2xl font-semibold"
        >
          {page.meta?.title || page.frontMatter?.title || page.name}
        </Link>
        <span className="text-md inline-block font-medium text-blue-500">
          <a href={page.frontMatter.url}>{"Unduh →"}</a>
        </span>
        {page.frontMatter?.date ? (
          <p className="text-md opacity-75">{page.frontMatter.date}</p>
        ) : null}
      </div>
    );
  });
}

<BeritaHeader />
<BeritaIndex />
